let num = 5;  // can be updated but not re-declared within the same scope
var greet = "Hi !"; // same as let, but can re-declared within the same scope
const pincode = 246746; // cannot be update nor re-declared

console.log(num);
console.log(greet);
console.log(pincode);